type Query {
  getTodos: [Todo]
}

type Mutation {
  addTodo(todo: TodoInput!): Todo
  updateTodo(id: ID!, title: String, completed: Boolean): Todo
}
type Todo {
  id: ID!
  title: String!
  completed: Boolean!
}

input TodoInput {
  id: ID!
  title: String!
  completed: Boolean!
}
